# 总结
①ODS层进行建模后得到DWD层，ODS层和DWD层分为`增量/全量/增量和变化/特殊表`。
②DWD层到DWS层的每日行为宽表(`设备/会员/商品/活动/地区 行为`)，需要从DWD层表中获取需要的字段进行聚合。
③DWS层到DWT层，计算 `首次末次时间/当天/累积/7天/30天/60天 ` 等统计数据；DWT层表不需要进行分区，因为会把旧数据和新数据合成一张表overwrite到表中。
④按主题进行聚合得到ads表(`设备/会员/商品/营销 主题`)，而每个主题都会有不同的指标进行统计，如果`设备包括活跃/新增/沉默/留存/连续登陆等信息`；ADS层表不需要分区，新数据计算后union查出来的旧数据(去重)，再overwrite插入，可以防止数据重复，减少小文件。

# 一、总览
## 1.1 关系模型
![电商数据库表关系图.png](https://upload-images.jianshu.io/upload_images/21580557-6c288fd9ab95bf94.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

## 1.2 维度模型(业务总线矩阵)：
![image.png](https://upload-images.jianshu.io/upload_images/21580557-d409f26886d77347.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

**建模流程：** `选择业务过程->声明粒度->确认维度->确认事实`
![业务总线矩阵](https://upload-images.jianshu.io/upload_images/21580557-e96cbc75b461e2e7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

1. `建宽表以维度为基准，每个维度对应一个主题宽表，宽表中的统计字段就是关联的事实表聚合后的度量值；`
2. 一般统计结果可以从主题宽表中直接获取或再进行简单的统计，如用户宽表如果需要根据性别统计，那么再关联一张用户性别表，进行简单统计即可。如果无法从宽表中获取的指标，就得从最细粒度的dwd层中进行统计。
3. DWS层存放的所有的主题对象当天的汇总行为，如每个地区当天的下单次数、下单金额等；  DWT层存放的是所有主题对象的累积行为，如每个地区最近7天的下单次数和金额。

<br>
**关系建模：**
- 优点：减少数据冗余，同时保证一次修改无需修改多个表，保证数据一致性。
- 缺点：获取数据需要join多张表，降低性能。

**维度建模：**
- 优点：1. 业务清晰 2. 减少join产生的shuffle过程，提高效率。

<br>
## 1.3 确定全量/增量/特殊表
![image.png](https://upload-images.jianshu.io/upload_images/21580557-51159929f98258c5.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
全量表：分区表，每天全量数据导入一个分区中；
增量表：分区表，每天增量数据导入一个分区中；
增量更新表：分区表，每天增量和更新数据导入一个分区中；  
用户表：`需要使用拉链表`，第一天导入全量数据，之后每天导入新增及变化数据；
特殊表：省份表、地区表、时间表等不会变动的表；
购物车表和收藏表数据量很大，但是为了方便维度建模，采用全量表形式导入。

## 1.4 事实表分类
1）事务型快照事实表(订单明细事实表/支付事实表/退款事实表/评价事实表)
以每个事务或事件为单位，例如一个销售订单记录，一笔支付记录等，作为事实表里的一行数据。一旦事务被提交，事实表数据被插入，数据就不再进行更改，其更新方式为增量更新。  
2）周期型快照事实表(加购事实表/收藏事实表)
周期型快照事实表中不会保留所有数据，只保留固定时间间隔的数据，例如每天或者每月的销售额，或每月的账户余额等。
3）累积型快照事实表(优惠券领用表/订单表)
累计快照事实表用于跟踪业务事实的变化。例如，数据仓库中可能需要累积或者存储订单从下订单开始，到订单商品被打包、运输、和签收的各个业务阶段的时间点数据来跟踪订单声明周期的进展情况。当这个业务过程进行时，事实表的记录也要不断更新。

# 二、业务
## 2.1 分层结构
- ODS（operation data store）：存放原始数据。保持数据原貌不变；创建分区表，防止后续的全表扫描；`时间分区`，采用`LZO压缩`，`指定输入输出格式`；创建外部表；导入表中用OUTPUTFORMAT，读取表用INPUTFORMAT。
- DWD（data warehouse detail）：结构粒度与ODS层保持一致，对ODS层数据进行清洗（去除无效数据、脏数据，数据脱敏，`维度退化`，`数据转换`等）。ETL数据清洗，用hive sql、MR、Python、Kettle、SparkSQL；`时间分区`，采用`LZO压缩`，采用`parquet格式`存储；创建外部表；
DWD层主要就是确定事实表和维度表dim。
- DWS（data warehouse service）：在DWD基础上，整合所有的维度和度量字段，按天进行轻度汇总。`时间分区`，采用`LZO压缩`，采用`parquet格式`存储；创建外部表；
DWS层主要就是以某一个维度轻度统计在业务总线矩阵中显示的与其关联的所有的事实表的度量值。DWS层存放的所有主题对象当天的汇总行为，例如每个地区当天的下单次数，下单金额等。
- DWT（data warehouse topic）：在DWS基础上，按主题进行汇总。`时间分区`，采用`LZO压缩`，采用`parquet格式`存储；创建外部表；
DWT层主要就是对DWS层的宽表的累积行为，例如每个地区最近7天（15天、30天、60天）的下单次数、下单金额等。
- ADS（Application Data Store）：为统计报表提供数据。`明确字段分割符`，与sqoop对应；创建外部表。

在ODS层对原始数据进行存储，需要采用压缩（一般lzo为10倍压缩），创建分区表，防止后续的全表扫描。

## 2.2 维度建模（ods->dwd）

### 2.2.1 日志表：
#### 2.2.1.1 日志结构
ODS层分为启动表、行为表和异常表：

![启动日志](https://upload-images.jianshu.io/upload_images/21580557-0843acfc0379489e.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![页面日志](https://upload-images.jianshu.io/upload_images/21580557-222149edf455f1ed.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

#### 2.2.1.2 日志解析
解析为五张表：启动日志表、页面日志表(用户访问的所有页面)、动作日志表(用户在每张页面上进行的操作)、曝光日志表()、错误日志表()。

![启动日志表](https://upload-images.jianshu.io/upload_images/21580557-2949bd89304d1589.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![页面日志表](https://upload-images.jianshu.io/upload_images/21580557-19a5d54c6356eba9.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![动作日志表](https://upload-images.jianshu.io/upload_images/21580557-ae77edc3b0834e48.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![曝光日志表](https://upload-images.jianshu.io/upload_images/21580557-5782ee7af63806fc.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

![错误日志表](https://upload-images.jianshu.io/upload_images/21580557-3ed5852fe2b4578e.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>错误日志表将启动日志和页面日志的异常信息都写入了表中，如果是启动日志异常，则只有启动日志的字段；如果是页面日志异常，则只有页面日志的字段。

<br>
### 2.2.2 业务表
**事务型事实表：**每个事务或事件为单位，数据就不再进行更改。
**周期型快照事实表：**不会保留所有数据，只保留固定时间间隔的数据
**累积型快照事实表：**累计快照事实表用于跟踪业务事实的变化。
#### 2.2.2.1 维度表
##### 2.2.2.1.1 特殊表合并：
`地区维度表：`将省份表和地区表合并作为地区维度表。

![地区维度表](https://upload-images.jianshu.io/upload_images/21580557-7a2e948beec35844.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

`时间维度表：`通过函数计算后导出到本地，然后导入到地区中。

![时间维度表](https://upload-images.jianshu.io/upload_images/21580557-8157ba4db7004e86.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

##### 2.2.2.1.2 全量表合并：
`商品维度表：`将当天的商品一、二、三级分类表、品牌表、SKU表和SPU表一共六张表合并为一张商品维度表。

![商品维度表](https://upload-images.jianshu.io/upload_images/21580557-38f8b4dccbc60de0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

`优惠券维度表：`将当天的优惠券表不作改动直接导入到优惠券维度表。

![优惠券维度表](https://upload-images.jianshu.io/upload_images/21580557-0cbe1facba657845.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)


`活动维度表：`将当天的活动表、优惠规则表(活动id和活动级别作为外键与事实表关联)、参与活动商品表合并为活动维度表。

![活动维度表](https://upload-images.jianshu.io/upload_images/21580557-9b591e21c69aba4c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

##### 2.2.2.1.3 拉链表：
`用户维度表:`用户表中**数据量大**不方便全量导入，同时每日数据既有新增又有变化但是频率不高，属于**缓慢变化维**，可以采用拉链表存储用户维度数据。
效果与全量表相同，可以通过过滤开始时间和结束时间来获取某个时间点的数据全量切片。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-d1106c8b1f86a922.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

**制作过程：**
1. 初始化拉链表(ods层导入全量)
2. 制作当日变动数据（ods层导入新增和变化）

**思路：**（当日新增或修改的数据作为新增数据插入） union all （原表中的数据left join当日数据，如果当日数据存在，则将结束时间改为昨天的日期）
```sql
insert overwrite table dwd_dim_user_info_his
    select 
        id,
        name,
        birthday,
        gender,
        email,
        user_level,
        create_time,
        operate_time,
        '2020-03-11' start_date,
        '9999-99-99' end_date
    from ods_user_info where dt='2020-03-11'

    union all 
    select 
        uh.id,
        uh.name,
        uh.birthday,
        uh.gender,
        uh.email,
        uh.user_level,
        uh.create_time,
        uh.operate_time,
        uh.start_date,
        if(ui.id is not null  and uh.end_date='9999-99-99', date_add(ui.dt,-1), uh.end_date) end_date
    from dwd_dim_user_info_his uh left join 
    (
        select
            *
        from ods_user_info
        where dt='2020-03-11'
    ) ui on uh.id=ui.id
```



<br>
#### 2.2.2.2 事实表
##### 2.2.2.2.1 增量同步
`支付事实表(事务型事实表)：`其中province_id需要与订单事实表join后获得。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-9e939d0aa9a18d20.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>维度表关系：通过user_id与用户维度表关联，通过province_id与地区维度表关联，通过分区字段date与时间维度表关联，度量值是payment_amount。

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **支付** | √        | √        | √        |          |            |          |          | 金额       |

<br>
***
`退款事实表（事务型事实表）：`字段相同，直接导入即可。如果需要province_id，可以关联订单事实表。
注意：因为订单事实表是增量与变化表，退货会改变订单状态并导入，所以只需要关联当天的数据即可。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-70388e8d80ce3f88.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>维度表关系：通过user_id与用户维度表关联，通过province_id与地区维度表关联，通过分区字段date与时间维度表关联，通过sku_id与商品维度表关联，度量值是payment_amount。

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **退款** | √        | √        |          | √        |            |          |          | 件数/金额  |

<br>
***
`评价事实表（事务型事实表）：`字段相同，直接导入当日记录即可。
注意：因为订单事实表是增量与变化表，如果关联评论事实表，则需要根据业务如评论关闭期限来选择订单事实表的日期。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-63d9c286362644ec.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **评价** | √        | √        |          | √        |            |          |          | 个数       |

<br>
***
`订单明细事实表（事务型事实表）：`需要关联订单事实表获取province_id，并计算原始价格和计算每件商品优惠后的价格以及分摊的运费和优惠（需要考虑误差，计算误差后将误差添加到第一条记录中）
![image.png](https://upload-images.jianshu.io/upload_images/21580557-7822ccf7fff21a09.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|              | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| ------------ | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **订单详情** | √        |          | √        | √        |            |          |          | 件数/原始金额/分摊优惠/分摊运费/分摊最终  |

<br>
##### 2.2.2.2.3 全量同步
`加购事实表（周期型快照事实表）：`字段相同，直接导入当日记录即可。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-2a8be0ee75596fc2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **加购** | √        | √        |          | √        |            |          |          | 件数/金额  |

<br>
****
`收藏事实表（周期型快照事实表）：`字段相同，直接导入当日记录即可。

![image.png](https://upload-images.jianshu.io/upload_images/21580557-bcedf27d77fea24f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **收藏** | √        | √        |          | √        |            |          |          | 个数       |


<br>
##### 2.2.2.2.3 增量和变化同步
`订单事实表（事务型事实表）：`
**难点：**订单关系型表中只有创建时间和修改时间，需要获得订单的支付时间、取消时间、完成时间、退款时间和退款完成时间，还需要活动id、优惠后金额、原始金额和运费。
**解决：**需要关联两张维度表。
订单状态相关字段填充需要关联订单状态表ods_order_status_log(增量)，表中记录了所有的订单状态，将同一订单的所有状态记录聚合为一个字段（使用concat_ws和map进行聚合，然后str_to_map转化为map结构）。
活动相关字段需要关联活动与订单关联表ods_activity_order(增量)，。

![image.png](https://upload-images.jianshu.io/upload_images/21580557-9e35f5497280baf9.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|          | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **订单** | √        | √        | √        |          |            | √        |          | 件数/金额  |

```sql
set hive.exec.dynamic.partition.mode=nonstrict;
insert overwrite table dwd_fact_order_info partition(dt)
select
    if(new.id is null,old.id,new.id),
    if(new.order_status is null,old.order_status,new.order_status),
    if(new.user_id is null,old.user_id,new.user_id),
    if(new.out_trade_no is null,old.out_trade_no,new.out_trade_no),
    if(new.tms['1001'] is null,old.create_time,new.tms['1001']),--1001对应未支付状态
    if(new.tms['1002'] is null,old.payment_time,new.tms['1002']),
    if(new.tms['1003'] is null,old.cancel_time,new.tms['1003']),
    if(new.tms['1004'] is null,old.finish_time,new.tms['1004']),
    if(new.tms['1005'] is null,old.refund_time,new.tms['1005']),
    if(new.tms['1006'] is null,old.refund_finish_time,new.tms['1006']),
    if(new.province_id is null,old.province_id,new.province_id),
    if(new.activity_id is null,old.activity_id,new.activity_id),
    if(new.original_total_amount is null,old.original_total_amount,new.original_total_amount),
    if(new.benefit_reduce_amount is null,old.benefit_reduce_amount,new.benefit_reduce_amount),
    if(new.feight_fee is null,old.feight_fee,new.feight_fee),
    if(new.final_total_amount is null,old.final_total_amount,new.final_total_amount),
    date_format(if(new.tms['1001'] is null,old.create_time,new.tms['1001']),'yyyy-MM-dd')
from
(
    select
        id,
        order_status,
        user_id,
        out_trade_no,
        create_time,
        payment_time,
        cancel_time,
        finish_time,
        refund_time,
        refund_finish_time,
        province_id,
        activity_id,
        original_total_amount,
        benefit_reduce_amount,
        feight_fee,
        final_total_amount
    from dwd_fact_order_info
    where dt
    in
    (
        select
          date_format(create_time,'yyyy-MM-dd')
        from ods_order_info
        where dt='2020-03-10'
    )
)old
full outer join
(
    select
        info.id,
        info.order_status,
        info.user_id,
        info.out_trade_no,
        info.province_id,
        act.activity_id,
        log.tms,
        info.original_total_amount,
        info.benefit_reduce_amount,
        info.feight_fee,
        info.final_total_amount
    from
    (
        select
            order_id,
            str_to_map(concat_ws(',',collect_set(concat(order_status,'=',operate_time))),',','=') tms
        from ods_order_status_log
        where dt='2020-03-10'
        group by order_id
    )log
    join
    (
        select * from ods_order_info where dt='2020-03-10'
    )info
    on log.order_id=info.id
    left join
    (
        select * from ods_activity_order where dt='2020-03-10'
    )act
    on log.order_id=act.order_id
)new
on old.id=new.id;
```

<br>
***
`优惠券领用事实表（周期型快照事实表）：`ods层需要导入当天领取/使用下单/使用支付的记录。因为订单状态是变化的，使用后需要修改原纪录，所以需要通过今天新增的数据获取

![image.png](https://upload-images.jianshu.io/upload_images/21580557-e39ed41d2d242cce.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

|                | **时间** | **用户** | **地区** | **商品** | **优惠券** | **活动** | **编码** | **度量值** |
| -------------- | -------- | -------- | -------- | -------- | ---------- | -------- | -------- | ---------- |
| **优惠券领用** | √        | √        |          |          | √          |          |          | 个数       |

```sql
set hive.exec.dynamic.partition.mode=nonstrict;
# 新增的数据插入到当天分区中，变化的数据修改回原数据中再覆写回原分区中
insert overwrite table dwd_fact_coupon_use partition(dt)
select
    if(new.id is null,old.id,new.id),
    if(new.coupon_id is null,old.coupon_id,new.coupon_id),
    if(new.user_id is null,old.user_id,new.user_id),
    if(new.order_id is null,old.order_id,new.order_id),
    if(new.coupon_status is null,old.coupon_status,new.coupon_status),
    if(new.get_time is null,old.get_time,new.get_time),
    if(new.using_time is null,old.using_time,new.using_time),
    if(new.used_time is null,old.used_time,new.used_time),
    date_format(if(new.get_time is null,old.get_time,new.get_time),'yyyy-MM-dd')
from
(
    # 获取将要修改的记录所在分区的全部数据
    select
        id,
        coupon_id,
        user_id,
        order_id,
        coupon_status,
        get_time,
        using_time,
        used_time
    from dwd_fact_coupon_use
    where dt in
    (
        select
            date_format(get_time,'yyyy-MM-dd')
        from ods_coupon_use
        where dt='2020-03-10'
    )
)old
full outer join
(
    # 当天的新增和变化数据
    select
        id,
        coupon_id,
        user_id,
        order_id,
        coupon_status,
        get_time,
        using_time,
        used_time
    from ods_coupon_use
    where dt='2020-03-10'
)new
on old.id=new.id;
```

<br>
## 2.3 日统计宽表（dwd->dws）
### 2.3.1 每日设备行为
**描述：**统计一个用户每天的登陆次数，以及访问的所有页面和页面的访问次数。
![每日设备行为](https://upload-images.jianshu.io/upload_images/21580557-a7b8b1b425a1eb2b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**从`dwd_start_log`表获取每个设备的登陆信息；从`dwd_page_log`表，对用户访问过的页面进行统计，使用`named_struct`和`collect_list`将访问页面信息信息组成列表得到`page_stats`字段。

### 2.3.2 每日会员行为
**描述：**统计一个用户在当天的所有行为统计，包括当天登陆次数，加购次数，下单次数，下单原始金额，下单优惠金额，下单最终金额，支付次数，支付金额，下单明细统计(统计了某商品的购买次数、购买件数、购买金额等)，还可以有退款、加购、点赞、收藏、领券等行为。

![每日会员行为](https://upload-images.jianshu.io/upload_images/21580557-51c166b68a63c9c3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**需要从`启动日志表`中获取用户当天登录次数；从`动作日志表`中获取用户当天加购/收藏等行为次数；从`订单事实表`中获取当天订单的下单次数和下单金额；从`支付事实表`中获取当天支付的次数和金额；从`订单详情事实表`中获取当天下单商品的总次数/件数/金额组成的数组(使用`named_struct('sku_id', sku_id, 'sku_num', sku_num, 'order_count', order_count, 'order_amount', order_amount)`构建结构体，group by user_id之后使用`collect_list`得到数组)。
**注意点：**存在非数字字段，不可以使用多表union all然后相加。
因此多表使用full out join进行关联，但是需要注意关联键是否为null的问题，使用nvl解决
`full outer join tmp_order on nvl(tmp_login.user_id,tmp_cart.user_id)=tmp_order.user_id;`
也要注意select时user_id为null的问题，使用coalesce解决`select coalesce(tmp_log.user_id,tmp_cart.user_id,tmp_order.user_id,tmp_payment.user_id,tmp_detail.user_id)` ；


### 2.3.3 每日商品行为
**描述：**统计一个商品在当天的所有信息，包括被下单信息、被支付信息、被退款信息、被加购收藏次数、评论信息。
![每日商品行为](https://upload-images.jianshu.io/upload_images/21580557-65ffb16c9cdad1c2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**
>1. 被下单信息从`dwd_fact_order_detail`表中统计；被支付信息需要先从当天和昨天的支付事实表中获取被支付的订单的`order_id`，然后从`dwd_fact_order_detail`中筛选出已被支付的订单；被退款信息从`dwd_fact_order_refund_info`表中获取，退款事实表中的一行表示一类商品被退款；被评价信息从`dwd_factor_comment_info`表中获取。
>2. 被加购和收藏信息需要从`dwd_action_log`表中获取，筛选出加购/收藏行为后group by sku_id进行统计。
>3. 因为是全数字字段，使用union all进行多表关联；也可以使用full out join多表关联，但是性能较低。

### 2.3.4 每日活动行为
**描述：**统计一个活动在当天有哪些订单参与、进行曝光和支付。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-4479abc0d6d97d21.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>1. 从活动维度表dws_dim_activity_info中获取当日活动的信息，
>2. 从dwd_fact_order_info表中获取下单和支付信息(因为支付可能跨天，所以从今天和昨天的分区表中查询处今天的下单和支付记录)。
>3. 从dwd_display_log表中获取活动曝光的次数。

### 2.3.5 每日地区行为
**描述：**统计每个省份的设备活跃数、下单信息和支付信息。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-727dc4b6a7361985.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**
>1. 活跃设备数来自于dwd_start_log启动日志表，通过area_code字段关联。
>2. 下单信息和支付信息来自于dwd_fact_order_info订单事实表，通过province_id进行关联，(因为支付可能跨天，所以从今天和昨天的分区表中查询处今天的下单和支付记录)。
>3. 因为启动日志表中是area_code，而订单事实表中是province_id，需要通过中间表dwd_dim_base_province地区维度表进行join。可以以地区维度表作为主表进行left join获取所有省份的信息，便于可视化展示。

<br>
## 2.4 主题宽表（dws->swt）
### 2.4.1 设备主题宽表
**描述：**统计设备的活跃时间等信息。

![设备主题宽表](https://upload-images.jianshu.io/upload_images/21580557-a31321b960edb02c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**
>1. 日志数据不像业务数据，没有历史数据。所以无法做初始化。
>2. 从dws_uv_detail_daycount中获取当天数据，与设备主题宽表进行全连接。


### 2.4.2 会员主题宽表
**描述：**统计登陆信息(首次末次、累计、30日)、下单信息(首次末次、累计、30日)、支付信息(首次末次、累计、30日)等。
![会员主题宽表](https://upload-images.jianshu.io/upload_images/21580557-a41af87d5e6bc68d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**
>1. 如果有历史数据，需要从dwd层查询数据对dwt_user_topic进行初始化。
>2. 信息从`dws_user_action_daycount`表中获取，最近30日的信息需要对最近30天的信息进行统计。

```
insert overwrite table dwt_user_topic
select
    nvl(new.user_id,old.user_id),
    if(old.login_date_first is null and new.login_count>0,'2020-03-10',old.login_date_first),
    if(new.login_count>0,'2020-03-10',old.login_date_last),
    nvl(old.login_count,0)+if(new.login_count>0,1,0),
    nvl(new.login_last_30d_count,0),
    if(old.order_date_first is null and new.order_count>0,'2020-03-10',old.order_date_first),
    if(new.order_count>0,'2020-03-10',old.order_date_last),
    nvl(old.order_count,0)+nvl(new.order_count,0),
    nvl(old.order_amount,0)+nvl(new.order_amount,0),
    nvl(new.order_last_30d_count,0),
    nvl(new.order_last_30d_amount,0),
    if(old.payment_date_first is null and new.payment_count>0,'2020-03-10',old.payment_date_first),
    if(new.payment_count>0,'2020-03-10',old.payment_date_last),
    nvl(old.payment_count,0)+nvl(new.payment_count,0),
    nvl(old.payment_amount,0)+nvl(new.payment_amount,0),
    nvl(new.payment_last_30d_count,0),
    nvl(new.payment_last_30d_amount,0)
from
dwt_user_topic old
full outer join
(
    select
        user_id,
        sum(if(dt='2020-03-10',login_count,0)) login_count,
        sum(if(dt='2020-03-10',order_count,0)) order_count,
        sum(if(dt='2020-03-10',order_amount,0)) order_amount,
        sum(if(dt='2020-03-10',payment_count,0)) payment_count,
        sum(if(dt='2020-03-10',payment_amount,0)) payment_amount,
        sum(if(login_count>0,1,0)) login_last_30d_count,
        sum(order_count) order_last_30d_count,
        sum(order_amount) order_last_30d_amount,
        sum(payment_count) payment_last_30d_count,
        sum(payment_amount) payment_last_30d_amount
    from dws_user_action_daycount
    where dt>=date_add( '2020-03-10',-30)
    group by user_id
)new
on old.user_id=new.user_id;
```

### 2.4.3 商品主题宽表
**描述：**统计商品被下单信息(累计、30日)、被支付信息(累计、30日)、被退款信息(累计、30日)、被加购和收藏信息(累计、30日)、被评论信息(累计、30日)
![image.png](https://upload-images.jianshu.io/upload_images/21580557-2a2b6c6d0076121a.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>**实现思路：**
>1. 如果有历史数据，需要从dwd层查询数据对dwt_user_topic进行初始化。
>2. 信息从`dws_sku_action_daycount`表中获取，最近30日的信息需要对最近30天的信息进行统计。

### 2.4.4 活动主题宽表
**描述：**统计参与活动的曝光次数(当日，累积)，下单次数/金额(当日，累积)，支付次数/金额(当日，累积)。因为活动时间较短，所以只统计全部累计信息。
![活动主题宽表](https://upload-images.jianshu.io/upload_images/21580557-4311501d2ee8b6a7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>1. 如果有历史数据，来自业务数据，需要从dwd层查询数据进行初始化。
>2. 新表与旧表进行全外连接，计算累计值。

### 2.4.5 地区主题宽表
**描述：**计算每个省的活跃设备(当天，最近30天)，下单次数/金额(当天，最近30天)，支付次数/金额(当天，最近30天)。
![image.png](https://upload-images.jianshu.io/upload_images/21580557-660d44fb91f012b2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>1. 如果有历史数据，来自业务数据，需要从dwd层查询数据进行初始化。
>2. 获取最近30天的数据进行计算得到新表后与旧表进行全外连(感觉直接用新表也行，毕竟dws层有所有省份的数据)。

<br>
## 2.5 主题表(dwt->ads)
### 2.5.1 设备主题
①`活跃设备数表`：日周月活统计
![image.png](https://upload-images.jianshu.io/upload_images/21580557-7c50f0767c874fe2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>从dwt_uv_topic表中获取设备最后一天登陆日期login_date_last，通过该字段进行统计。查询出所有旧数据然后union新数据进行去重，然后overwrite插入表中(可以重复执行任务而不会导致数据重复，并且减少了小文件)。
>![image.png](https://upload-images.jianshu.io/upload_images/21580557-c2991b0d1179e9fd.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

<br>
②`每日新增设备`：每天新增设备
![image.png](https://upload-images.jianshu.io/upload_images/21580557-051dfe6d6a27c0ed.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>从dwt_uv_topic表中统计首次登陆日期login_date_first为当天的设备记录进行统计。
>同样可以查询所有数据union最新数据overwrite插入表中。

<br>
③`沉默用户`：只在安装当天启动过，且启动时间在七天前
![image.png](https://upload-images.jianshu.io/upload_images/21580557-dfb43c8763aff7d6.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>从dwt_uv_topic表中统计首次登陆日期login_date_first与login_date_last为同一天，且日期在七天前的设备。

<br>
④`本周回流用户`：上周未活跃，本周活跃(但不是新增用户)
![image.png](https://upload-images.jianshu.io/upload_images/21580557-6ab38ff141273604.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>从dwt_uv_topic表中获取login_date_last在本周的活跃用户，排除掉login_date_first在本周的新增用户，然后leftjoin上周的活跃用户，排除掉上周活跃用户就是本周回流用户。

<br>
⑤`流失用户数`：最近7天未活跃的设备
![image.png](https://upload-images.jianshu.io/upload_images/21580557-665ed2f21987dced.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>从dwt_uv_topic表中筛选出login_date_last在七天前的数据并进行统计。

<br>
⑥`留存率`：1/2/3/4/5/6/7/15/30/60日留存率，
![image.png](https://upload-images.jianshu.io/upload_images/21580557-0ad6a6fd52276179.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
 
<br>
⑦`最近连续三周活跃用户数`
![image.png](https://upload-images.jianshu.io/upload_images/21580557-9259350d62615547.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
>未优化的方案：不能从dwt_uv_topic表中计算，所以从dws_uv_detail_daycount表中进行统计；统计本周、上周和上上周的登陆用户，然后进行inner join得到连续三周活跃的设备。
>以上方案join性能消耗较大，推荐使用以下方案：同样需要统计本周、上周和上上周的登陆用户去重，然后union all，再groupby mid，如果sum(mid)等于3，表示该设备连续三天登陆。

<br>
⑧`最近七天内连续三天活跃用户数`


<br>
### 2.5.2 会员主题

<br>
### 2.5.3 商品主题

<br>
###2.5.4 营销主题（用户+商品+购买行为） 
